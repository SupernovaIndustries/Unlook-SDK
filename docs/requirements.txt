# Core documentation requirements - minimal for ReadTheDocs
sphinx>=5.0.0,<7.0.0
sphinx_rtd_theme>=1.2.0
sphinx-copybutton>=0.5.0
myst-parser>=0.18.0

# Basic dependencies needed for imports (lightweight versions)
numpy>=1.19.0
pillow>=8.0.0

# Note: Heavy dependencies like opencv-python, open3d, mediapipe, torch, etc.
# are mocked in conf.py to avoid installation issues on ReadTheDocs