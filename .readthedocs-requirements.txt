# Minimal requirements for ReadTheDocs build
# This file can be used as an alternative to docs/requirements.txt

# Core Sphinx
sphinx>=5.0.0,<7.0.0
sphinx_rtd_theme>=1.2.0
sphinx-copybutton>=0.5.0
myst-parser>=0.18.0

# Minimal dependencies to avoid import errors
numpy>=1.19.0
pillow>=8.0.0
pyyaml>=6.0
tqdm>=4.60.0
msgpack>=1.0.2
scipy>=1.5.0

# Note: All heavy dependencies (opencv, open3d, mediapipe, etc.) 
# are mocked in docs/source/conf.py