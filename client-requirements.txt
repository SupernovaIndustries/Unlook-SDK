# UnLook SDK Client Requirements
# These packages are needed to run the UnLook client applications

# Core requirements
numpy>=1.20.0
opencv-contrib-python>=4.5.0  # For structured light pattern generation and decoding
pyzmq>=22.0.0
msgpack>=1.0.2
pyyaml>=6.0

# For 3D scanning and visualization
open3d>=0.19.0     # For mesh generation and point cloud processing
matplotlib>=3.4.0  # For data visualization
cgal>=5.3.0        # For professional-grade triangulation (optional but recommended)

# For examples and utilities
pillow>=8.0.0      # Image processing
tqdm>=4.60.0       # Progress bars
scipy>=1.10.0      # Scientific computing

# GPU acceleration (required for optimal performance)
# Uncomment the appropriate line based on your CUDA version:
#cupy-cuda11x>=12.0.0  # For CUDA 11.x
cupy-cuda12x>=12.0.0  # For CUDA 12.x

# For neural network enhancement (MVS2D model)
tensorflow>=2.11.0  # For TensorFlow models
torch>=2.0.0       # Will work on CPU, but CUDA recommended
torchvision>=0.16.0  # Required for models and transformations
tensorboard>=2.11.0  # For visualizing training progress

# For saving point clouds in diverse formats
h5py>=3.1.0        # For HDF5 file format support

# Optional: for development and testing
pytest>=7.0.0      # For running tests
mypy>=1.0.0        # For type checking

# Instead of python-pcl, we use Open3D for all point cloud processing

mediapipe>=0.10.0  # For image processing and visualization
reportlab>=3.6.0  # For PDF generation
ultralytics>=8.0.0  # For YOLO model inference and hand detection